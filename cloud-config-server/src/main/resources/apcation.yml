server:
  port: 9296

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: file:/home/dknitk/Public/gitRepository/config-server
          force-pull: true
